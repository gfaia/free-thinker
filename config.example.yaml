database:
  driver: sqlite
  dsn:    data/aggregator.db

storage:
  content_root: data/articles

schedule:
  cron: "0 2 * * *"

fetchers:
  - name: zhihu
    enabled: true
    queries:
      - golang
      - distributed systems
    config:
      max_pages: 3
      rate_limit_ms: 800
      # user_agent: "..."
      # cookie:   "..."

  # - name: weibo
  #   enabled: false
  #   queries:
  #     - golang
  #   config: {}
